{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py loaddata initial_geo.json && python manage.py loaddata initial_officers.json && python manage.py init_admin && python manage.py collectstatic --noinput && gunicorn dms_project.wsgi:application --bind 0.0.0.0:${PORT:-8000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
